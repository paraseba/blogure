;(ns blogure.generate
  ;(:require [blogure.core :as core])
  ;(:gen-class))

;(defn -main [& config-file]
  ;(let [config-file (or (first config-file) "blogure_config.clj")]
    ;(core/generate (load-file config-file))))
